# FB Automation State

Оновлено: 2026-08-12

## Джерела істини

Пріоритет:

1. Нове явно затверджене рішення в поточному чаті.
2. `Регламент_Facebook_Ads_PL_v1.0.docx`.
3. Поточна реалізація у Python-скриптах.
4. Старі чати, брифи та попередні рекомендації.

Цей файл є технічним журналом стану. Він не замінює регламент.

## Токени та system users

- `FB_ACCESS_TOKEN` → `for_rules` → `fb_manager.py`, `fb_hygiene.py`.
- `FB_SCALER_ACCESS_TOKEN` → `for_scaler` → `fb_duplicate_test.py` і майбутній `fb_scaler.py`.
- Токени та ролі не змішувати.

## Затверджена модель дублювання

Scaler має створювати в тій самій кампанії:

- новий AdSet;
- нове Page Video;
- новий Creative через `object_story_spec.video_data`;
- нове оголошення у новому AdSet;
- без передачі `object_story_id` існуючої публікації;
- у статусі `PAUSED` під час тестів.

Справжню нову копію підтверджує одночасне виконання умов:

- `new_ad_id != source_ad_id`;
- `new_creative_id != source_creative_id`;
- `copied_adset_id != source_adset_id`;
- нове оголошення належить новому AdSet;
- нове оголошення використовує новий Creative;
- Creative створений через `object_story_spec`, а не `object_story_id`;
- новий Creative використовує новий `video_id`;
- у Ads Manager вибрано «Створити оголошення», а не «Використовувати існуючу публікацію».

## Результати тестів

### v26.3 — відхилено

- Новий AdSet було створено.
- Creative створювався через `object_story_id`.
- Ads Manager показував «Використовувати існуючу публікацію».
- Такий спосіб не відповідає задачі дублювання оголошення.

### v26.4 — структура нового дубля підтверджена

Джерело:

- AdSet: `120247989505660301`
- Ad: `120247989505540301`
- Creative: `1785909172594984`
- root video: `2189511635226791`

Створено:

- AdSet: `120248233771070301`
- Ad: `120248233772070301`
- Creative: `1471180001441578`
- Page Video: `1848696159451925`

Підтверджено:

- усі структурні ID нові;
- новий Ad знаходиться у новому AdSet;
- новий Ad використовує новий Creative;
- Creative створено через `object_story_spec.video_data`;
- `object_story_id` існуючої публікації у запит Creative не передавався;
- текст, CTA, URL і Pixel збігаються;
- Ads Manager показує «Створити оголошення»;
- оголошення створено без API-помилки.

Важливо: загальний `true_duplicate_ok` у звіті v26.4 дорівнює `false`, бо API-аудит знайшов одну розбіжність AdSet:

- у джерелі `targeting_automation.individual_setting` містить `age: 1` і `gender: 1`;
- у копії це вкладене поле відсутнє;
- явні `age_min`, `age_max`, гео, плейсменти та платформи у звіті збережені.

Після v26.5.2 користувач перевірив новий AdSet в Ads Manager і підтвердив, що
налаштування групи правильні. Тому відсутність `individual_setting` у повторному
GET-відгуку класифікуємо як UI-підтверджену нормалізацію readback для цього
сценарію. Розбіжність залишається видимою у JSON як warning, але сама по собі
не блокує тест.

### v26.5.1 — відхилено: Meta вимагає мініатюру

Тест навмисно не передавав `image_url` або `image_hash`, щоб перевірити,
чи залишить Meta вибір мініатюри в режимі `Авто`.

Результат:

- нове Page Video створено: `1364327572519044`;
- новий AdSet створено: `120248234118690301`;
- Creative не створено;
- Meta повернула `code=100`, `subcode=1443226`;
- вимога Meta: передати `image_hash` або `image_url` у `object_story_spec.video_data`.

Висновок: для цього сценарію створення нового Creative через
`object_story_spec.video_data` режим без передавання мініатюри технічно не
працює. Це підтверджено фактичним API-викликом, а не припущенням.

### v26.5.2 — перший запуск не тестував дублювання

Перший запуск було виконано з неправильним source AdSet:

- передано `120248234118690301` — залишковий порожній AdSet, створений v26.5.1;
- правильне джерело — `120247989505660301`;
- у переданому AdSet знайдено `0` оголошень;
- скрипт зупинився на перевірці джерела з повідомленням
  `Expected exactly 1 ad in source adset; got 0`;
- нові AdSet, Page Video, Creative або Ad у цьому запуску не створювалися;
- `cleanup: []` є правильним результатом, бо очищувати після цього запуску нічого.

Це помилка вхідного ID, а не помилка токена, дозволів, Meta-generated thumbnail
або механізму дублювання. Результат гіпотези v26.5.2 залишається невідомим до
повторного запуску на правильному джерелі.

### v26.5.2 — дубль створено, гіпотезу мініатюри відхилено

Повторний запуск виконано з правильним source AdSet `120247989505660301`.

Створено:

- Page Video: `1031980492959285`;
- AdSet: `120248234293180301`;
- Creative: `1367411501557215`;
- Ad: `120248234294370301`.

Підтверджено:

- новий Ad знаходиться у новому AdSet;
- новий Ad використовує новий Creative;
- Creative створено через `object_story_spec` без `object_story_id`;
- текст, CTA, URL і Pixel збережені;
- помилок доставки після створення немає;
- налаштування AdSet в Ads Manager користувач перевірив як правильні.

Результат перевірки мініатюри:

- скрипт не завантажував власну обкладинку і не змінював `is_preferred`;
- `image_url` був узятий з кадру, автоматично згенерованого Meta;
- Ads Manager усе одно показав режим `Вручну`;
- отже, походження кадру не зберігає режим `Авто`, якщо `image_url` явно
  передано при створенні Creative.

Висновок: гілка ручного створення нового Creative через `object_story_spec`
не дає потрібної поведінки мініатюри. Наступний тест переходить на нативний
Ad Copies API.

### v26.6 — відхилено: deprecated standard_enhancements

- AdSet копіюється через `POST /{adset_id}/copies`;
- Ad копіюється через `POST /{source_ad_id}/copies` у новий AdSet;
- creative-параметри не передаються;
- `object_story_id`, `image_url` та `image_hash` не передаються;
- Meta сама вирішує, чи повторно використати незмінний Creative або створити
  новий Creative;
- обов'язково має бути новий Ad ID у новому AdSet;
- у UI перевіряємо `Створити оголошення` та мініатюру `Авто`.

Фактичний результат:

- скопійований AdSet `120248234920820301` було створено;
- Meta відхилила створення Ad на етапі `POST /{source_ad_id}/copies`;
- помилка: `code=100`, `subcode=3858504`;
- причина: вихідний Creative містить застарілий пакет
  `standard_enhancements`, який більше не підтримується при копіюванні;
- тестовий AdSet після помилки успішно видалено автоматичним cleanup;
- новий Ad або Creative у цьому запуску не створено.

Висновок: нативний `/copies` без `creative_parameters` не сумісний із цим
вихідним Creative. Гіпотезу v26.6 відхилено фактичним API-викликом.

### v26.6.1 — частковий технічний успіх, але publish FAIL

- AdSet і далі копіюється нативно через `POST /{adset_id}/copies`;
- Ad копіюється нативно через `POST /{source_ad_id}/copies`;
- з вихідного `degrees_of_freedom_spec` видаляється лише заборонений
  `standard_enhancements`;
- його `enroll_status` переноситься в актуальні окремі функції для відео:
  `video_auto_crop`, `text_optimizations`, `inline_comment`;
- очищений `degrees_of_freedom_spec` передається через офіційний параметр
  Ad Copies API `creative_parameters`;
- `object_story_id`, `image_url`, `image_hash`, нове Page Video та ручна
  мініатюра не передаються;
- якщо API не поверне старий пакет або його `OPT_IN/OPT_OUT`, тест зупиниться
  з діагностикою, а не підставить вигадане значення.

Фактичний запуск: GitHub Actions run `#44`, commit
`a296b23d486f03e1c8576f596764e6c3a7dae902`.

Створено:

- AdSet: `120248236576670301`;
- Ad: `120248236577940301`;
- Creative: `1897098357917433`;
- Meta створила новий Creative, а не повторно використала source Creative.

Підтверджено:

- помилку `3858504` усунуто;
- `standard_enhancements` не передавався;
- source `OPT_IN/OPT_OUT` для вже наявних індивідуальних функцій збережено;
- новий Ad знаходиться у новому AdSet;
- Pixel збережено: `1221875848516044`;
- AdSet fidelity пройдено; відсутній у readback
  `targeting_automation.individual_setting` залишився UI-підтвердженим warning;
- текст, заголовок, опис, CTA, посадкова сторінка, `url_tags` та `image_hash`
  збережені;
- `object_story_id`, `image_url` та `image_hash` скрипт у запит `/copies`
  не передавав.

Не пройдено:

- новий Ad перейшов у `effective_status=WITH_ISSUES`;
- hard error: `2643026` — загальна помилка обробки оголошення;
- Creative має `status=WITH_ISSUES`;
- `issues_info` містить одну hard error;
- під час раннього readback у нового Ad були відсутні `tracking_specs` і
  `conversion_specs`;
- root `video_id` у нового Creative інший, при цьому story video, текст,
  CTA та image hash збережені; це фіксуємо як факт, але не називаємо причиною
  помилки без окремого доказу;
- `true_duplicate_ok=false`, `publish_probe_ok=false`.

Важливо: GitHub Action завершився зеленим, бо Python виконався без винятку й
зберіг звіт. Бізнес-вердикт тесту при цьому `FAIL` через стан оголошення.

Висновок: v26.6.1 довів, що deprecated enhancements можна мігрувати через
`creative_parameters` і отримати нативні нові Ad та Creative. Але цей дубль
не готовий до production, бо Meta не змогла обробити новий Creative.

### v26.7-stable — повернення до робочого object_story_spec

Після фактичного `publish FAIL` у v26.6.1 затверджено не блокувати Scaler
нативним Ad Copies API. Робочою базою знову є перевірений механізм v26.5.2:

- нове Page Video завантажується з бінарного джерела;
- Meta генерує кадри для нового відео;
- один Meta-generated frame явно передається як `image_url`, бо без
  `image_url` або `image_hash` Meta відхиляє новий Creative;
- новий Creative створюється через `object_story_spec.video_data`;
- `object_story_id` існуючої публікації не передається;
- створюються нові AdSet, Page Video, Creative та Ad;
- усі тестові об'єкти залишаються `PAUSED`;
- відома прийнята відмінність: Ads Manager показує мініатюру `Вручну`;
- native Ad Copies залишається лише експериментальною гілкою в історії Git.

У v26.7-stable також збережено аудит сирої різниці AdSet, але підтверджене
користувачем зникнення лише
`targeting_automation.individual_setting={age: 1, gender: 1}` у readback
класифікується як warning і не робить бізнес-тест хибно негативним.

Фактичний контрольний запуск користувач підтвердив як успішний. Отже,
`v26.7-stable` зафіксовано як робочу технічну базу Scaler із прийнятим
обмеженням мініатюри `Вручну`.

### fb_scaler.py v1.0-test — планування та один PAUSED-дубль

Перший Scaler-етап навмисно не має production-активації.

- `plan` аналізує сьогодні + вчора, агрегує offer_id між усіма налаштованими
  акаунтами з нормалізацією USD/PLN і не виконує жодного запису в Meta;
- застосовуються source-пороги, шкала `12/15/18`, lead caps, offer
  multipliers, Cost Goal/Bid Cap multipliers і caps `50/150`;
- `ктг` повністю виключено;
- пріоритет caps: більше лідів, потім нижчий `CPL/BE`;
- фактична bid strategy береться з API, а маркер `кос/бід` виправляється лише
  в імені нового дубля;
- дробова кількість після множників округлюється вниз, щоб не перевищувати
  затверджений множник;
- production marker: дата + source AdSet ID + sequence;
- jitter ставки детермінований у межах ±0,5–1%;
- start_time детерміновано потрапляє у вікно 05:35–05:55 наступного дня за
  Europe/Warsaw;
- `test_one_paused` потребує явного source AdSet і контрольної фрази, створює
  максимум один дубль та залишає нові AdSet і Ad у `PAUSED`;
- цей тест перевіряє readback `start_time`, але ще не перевіряє автоматичну
  активацію scheduled-дубля;
- duplication path повторно використовує без змін підтверджений
  `fb_duplicate_test.py` v26.7-stable.

## Поточний наступний крок

1. Після окремого підтвердження доступу нового workflow до Meta/Telegram
   secrets у публічному репозиторії додати безпечний workflow без GitHub
   artifact зі статистикою.
2. Запустити Scaler у режимі `plan`.
3. Перевірити Telegram та локальний JSON: кандидати, source/offer CPL/BE, кількість до
   та після множників, caps і bid/name mismatches.
4. Після перевірки plan запустити `test_one_paused` для одного погодженого
   source AdSet із фразою `CREATE_ONE_PAUSED_DUPLICATE`.
5. Перевірити новий AdSet/Ad у Ads Manager, readback `start_time`, budget,
   bid strategy, Pixel, jitter ставки та відсутність delivery issues.
6. Лише після цього окремо тестувати статус `ACTIVE` з майбутнім start_time і
   фактичний автоматичний старт. Production cron о 16:00 поки не створювати.

## Не чіпати

- `for_rules`;
- `FB_ACCESS_TOKEN`;
- `fb_manager.py`;
- `fb_hygiene.py`;
- production-логіку правил до завершення тестів Scaler.
